version: '3.9'

services:
  postgres:
    image: postgres:15
    container_name: qa_postgres

    environment:
      POSTGRES_DB: hillel_db
      POSTGRES_USER: admin
      POSTGRES_PASSWORD: admin

    ports:
      - "5432:5432"